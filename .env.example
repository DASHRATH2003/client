## Frontend (Vite) environment variables

# Production/Preview: absolute backend base URL (Render)
# Set this on Vercel Project → Settings → Environment Variables
VITE_API_BASE=https://server-1-4bfp.onrender.com/api

# Local development:
# You can omit VITE_API_BASE so the app uses the proxy `/api`
# defined in `vite.config.js` to hit `http://localhost:5000`.